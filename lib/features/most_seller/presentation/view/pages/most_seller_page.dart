import 'package:flutter/material.dart';
    
class MostSellerPage extends StatelessWidget {
  const MostSellerPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('MostSeller page')),
    );
  }
}
