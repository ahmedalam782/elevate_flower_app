import 'package:flutter/material.dart';
    
class OccasionsPage extends StatelessWidget {
  const OccasionsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('occasions page')),
    );
  }
}
